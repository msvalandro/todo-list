import { StyleSheet } from 'react-native'

export const styles = StyleSheet.create({
  container: {
    paddingHorizontal: 20,
    alignItems: 'center',
  },
  textBold: {
    marginTop: 16,
    fontWeight: 'bold',
  },
  text: {
    fontSize: 14,
    color: '#808080',
  },
})
